name: KuroPlayer
options:
  bundleIdPrefix: com.kuroplayer
targets:
  KuroPlayer:
    type: application
    platform: macOS
    deploymentTarget: "15.0"
    sources:
      - Sources/KuroPlayer
    settings:
      CODE_SIGN_IDENTITY: ""
      CODE_SIGNING_REQUIRED: "NO"
      CODE_SIGNING_ALLOWED: "NO"
      PRODUCT_BUNDLE_IDENTIFIER: com.kuroplayer.app
      INFOPLIST_FILE: KuroPlayer.app/Contents/Info.plist
